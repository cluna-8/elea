Assets del branding pack del DISTRIBUIDOR (logo.png, favicon…): se montan
en runtime en /srv/branding junto a brand.json — jamás horneados (never fork).
El logo default Sentinel vive en el bundle (frontend/src/logo.png).

fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.31
psycopg2-binary==2.9.9
pydantic==2.13.4
pydantic-settings==2.3.3
httpx==0.28.1
# Explícito y no transitivo (vía starlette/httpx): el `finally` de `_StreamConTurno`
# (src/api/gateway.py, issue #150) depende de la semántica de `move_on_after(shield=True)`
# de anyio 4. Pineado a la versión que ya trae la imagen hoy.
anyio==4.14.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
pyyaml==6.0.1
alembic==1.13.3
cryptography==42.0.8
# Cliente OIDC genérico para el contrato de proveedor SSO (spec 017 US2, research D2).
authlib==1.6.5
redis==5.0.8
tiktoken==0.13.0
# Módulo de compresión local headroom (SmartCrusher, Rust) — sin tokens, sin torch.
# Solo la base (no [ml]/[proxy]): comprime contenido estructurado (JSON/logs/RAG/arrays).
headroom-ai==0.30.0
# Test stack (F0-7 smoke tests)
pytest==8.2.2
pytest-asyncio==0.23.7

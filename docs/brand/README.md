# Brand-packs GENERADOS del sitio de docs (022 US3)

Cada subdir `<slug>/` sale de `deploy/release/render_docs_brand.sh <slug> <brand.json>` —
derivado del brand-pack de la 020 (una sola fuente de marca). `aegis/` es el ejemplo commiteado.

# MIGRADO al sitio de producto (spec 022)

Este contenido vive ahora en el sitio de documentación (`docs/docs/integrations/`) — la ÚNICA
fuente. Preview local: `mkdocs serve` en `docs/`; imagen: `make -C deploy build-docs`.
Este stub queda solo para no romper links históricos del repo.
